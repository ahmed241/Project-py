[project]
name = "project-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "manim-narration[full]",
]

[tool.uv.sources]
manim-narration = { git = "https://github.com/maejam/manim-narration.git" }
